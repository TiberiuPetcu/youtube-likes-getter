# youtube likes getter 👍
Python script that gets the likes of a certain video

# Run instructions
Run `pip install -r requirements.txt`  
Use tool with `python yt.py [VIDEO_ID]`, for example `python yt.py dQw4w9WgXcQ`
